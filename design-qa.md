# Orca Research 界面验收

更新日期：2026-09-22

## 本轮方向

依据用户反馈，以实际研究操作和报告阅读为中心。去掉宣传口号与闲置报告大面板，保留炭灰导航、浅色工作区和低饱和绿色。frontend-design 技能用于收紧阅读层级、间距和移动端布局。

## 已实现

- 新建研究页提供问题输入、快捷问题和近期报告；创建任务后显示研究进程与报告。
- 研究题目随任务保存，刷新恢复时立即显示；忽略已关闭事件流的迟到消息。
- 研究进程使用自然语言，不直接展示反思 JSON；阅读记录可打开原始来源。
- 报告支持复制 Markdown；引用按正文 AST 转换，不修改代码和已有链接。
- 来源列表显示编号、标题和网址；仅允许 HTTP/HTTPS 引用链接。
- 收紧报告标题和元数据区域，宽表格提供独立横向滚动区。
- 历史报告支持搜索、详情直达、刷新及浏览器前进后退；后退保留搜索条件。
- 历史列表首屏显示 20 条，可继续加载；请求取消和迟到响应有保护。
- 手机布局取消多层嵌套留白；动效仅用于短暂切换，并尊重减少动态效果设置。
- FadeContent 是参考 React Bits 思路的轻量实现，没有引入整套动画依赖。

## 验收证据

- 浏览器使用现有数据库的临时副本，共 223 份报告；未修改原始数据库，也未提交新研究。
- 检查报告 223 和 208 的实际正文、连续引用和来源列表。
- 搜索 CORS 得到 5 条记录；打开详情、刷新、后退保留搜索、前进恢复详情均通过。
- 390 × 844 手机视口截图已检查：元数据两列、正文换行和页面边距正常；验收后恢复默认视口。
- 浏览器读取到的错误日志为空。
- 自动化测试：3 个文件、20 项通过，覆盖报告引用、任务恢复、旧事件流隔离和历史导航竞态。
- git diff --check 通过。
- npm run build 通过，包括类型检查和静态页面生成。

## 验收边界

- 本轮没有运行付费搜索或模型调用；完整的新研究生成链路没有重新做端到端验证。
- 活跃任务与 SSE 恢复通过自动化测试验证，未宣称真实运行中的视觉状态已全部验收。
- 宽表格滚动已实现，但所选真实报告不含表格，未完成该状态的浏览器视觉验证。
- 临时验收后端已停止，避免用户误将新任务写入临时副本。
