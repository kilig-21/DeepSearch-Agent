# Orca Research — 项目纪律(所有会话必读)

## 唯一事实源

- 计划书:`PLAN.md`(v1.3,三轮评审冻结)。开工前先读 **§0**(背景与已确认决策链 D1-D6)与 **§4**(当前阶段任务与验收标准)
- 实测记录:`apps/api/docs/probe_results.md`;来源集合:`docs/SOURCES.md`(集合外站点不抓正文)

## git 纪律

- **git commit 一律禁止 `-c user.name` / `-c user.email` 临时参数**。仓库级身份已配置(李红鲜 <995677570@qq.com>),裸提交即可
- **push 到 GitHub 前必须先征得用户确认**,不得自行 push
- `.env` 与 `data/` 严禁入库(`.gitignore` 已覆盖,勿改动);发现身份异常立即报告用户
- 每完成一个可验证的块(测试通过)即 commit

## 执行纪律

- 不推翻 §0 决策链;不实现 §4 Non-Goals 清单中的内容
- 遇到阻塞、依赖失败或指令模糊:**停下来问用户,不要猜**
- 所有对话与回复使用中文
